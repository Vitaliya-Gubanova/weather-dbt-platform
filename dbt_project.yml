name: 'weather_platform'
version: '1.0.0'
config-version: 2

profile: 'weather_platform'

model-paths: ["models"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  weather_platform:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts